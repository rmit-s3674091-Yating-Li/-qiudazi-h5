# V7 Workboard

> V7 开发阶段的机器可消费工作状态真源。产品规则仍以各 canonical baseline 为准；本文件只记录“当前要做什么 / 做到哪一步 / 为什么被阻塞”。

## 状态机

- `TODO`：尚未实现，可被 Builder 领取。
- `IN_PROGRESS`：Builder 当前唯一开发切片。
- `NEEDS_VERIFY`：已实现并自检，等待独立 Verifier。
- `CODE_REOPEN`：Verifier 已确认 current implementation 存在真实代码/产品缺口；Builder 最高优先级消费。
- `INFRA_BLOCKED`：实现或验证被 CI/runner/外部基础设施阻塞；不得占住 Builder 队列。
- `EXTERNAL_BLOCKED`：依赖外部凭据/服务；不得占住 Builder 队列。
- `BROWSER_PENDING`：低层证据已足够，必须留到 Browser/真机阶段。
- `VERIFIED`：开发阶段独立验证已完成。

## 证据规则

开发阶段采用 **affected-scope evidence**：head 前移后，仅当新 commit 影响该功能的代码、schema、RPC、测试预期或 canonical AC 时，既有验证才失效。纯文档/不相关模块提交不会机械抹掉其他模块的开发期证据。

进入 Candidate Freeze 后切换为 **strict exact-SHA evidence**：Unit / Integration / Build / Preview / Browser / Gate 必须属于同一 candidate SHA。

## 当前 V7 队列

| ID | Area | Priority | State | Current evidence / blocker | Next owner/action |
|---|---|---:|---|---|---|
| V7-SCORE-01 | Scoring transaction/idempotency | P0 | INFRA_BLOCKED | stable operation UUID/lost-response 相关实现已存在；真实 DB Integration 仍依赖 isolated local Supabase runner | Infra 恢复后 Verifier 跑真实 DB behavior；Builder 不原地等待 |
| V7-QUICK-01 | Quick started exit → Event finish | P0 | VERIFIED | independent affected-scope verification confirms exact head `a403e181ee6811743dc100d556198534369e470d` Integration Contract #231 PASS；该 run 实际完成 isolated local Supabase startup、clean repository migration replay 与 integration contract tests，且该 head 的 Domain Unit #235、H5 Build #637 同时 PASS。随后到核验 head `64d9b0898d75ec5f2391be5525c668b4560d1af5` 仅 Workboard 状态记录发生变化，未影响 Quick code/schema/RPC/test/canonical，因此 same-scope DB evidence 继续有效 | Done；后续仅当 Quick started-exit code/schema/RPC/test expectation/canonical AC 被修改时重新核验 |
| V7-INFRA-EDGE-01 | Supabase Edge Runtime / local Integration startup | P0 | VERIFIED | independent verification confirms exact head `54c929ff1d84c434ef9cbe01f945ed2ac7e77963` Integration #227 已成功完成 isolated local Supabase startup 与 clean migration replay 后才进入真实 integration behavior；更晚 exact head `a403e181ee6811743dc100d556198534369e470d` Integration #231 又完整通过 startup、clean replay 与 integration contract tests，证明 Edge-loaded graph 修复在后续受影响分支状态仍可启动且未被回归。没有证据显示后续 Workboard-only commit 影响 Edge runtime | Done；后续仅当 Edge-loaded module graph、Supabase function/runtime config 或相关 integration harness 被修改时重新核验 |
| V7-ID-01A | Identity sign-out boundary/cache isolation | P1 | VERIFIED | real `supabase.auth.signOut()` + identity/profile/guest/pending/session storage 清理 + query/in-flight cache clear + `loginRequired`→显式 `/login` boundary 已落地；`unit-identity-session.mjs` 已接入 `test:unit`，Domain Unit #156 PASS。仅验证本切片，不包含 V7-ID-01B nickname exchange/account switch | Done；后续不相关 commit 不机械失效 |
| V7-ID-01B | Nickname identity exchange/account switch | P1 | INFRA_BLOCKED | affected-scope 静态实现与 Domain Unit #167 已通过：test-only Edge 使用 service-role-only normalized nickname lookup，仅返回 one-time magic-link hash，客户端以 `verifyOtp` 恢复 canonical session；DB normalization/唯一索引与 lookup 权限边界已落地。真实 migration replay、duplicate normalized nickname、existing/missing exchange、失败无半登录、刷新后 Profile 绑定仍需 DB Integration；current Integration #163 在 `Start isolated local Supabase` 失败，后续 replay/contracts skipped | Infra 恢复后 Verifier 跑 identity DB/Edge Integration；Browser 阶段再验证 G→老郑私有数据隔离 |
| V7-PHOTO-01 | Event photo → personal album / save to phone | P1 | BROWSER_PENDING | affected-scope 低层证据闭环：赛事相册与“参与赛事相册”均有显式单张“保存到手机”；优先 Web Share files，普通浏览器降级 anchor download，受限 WebView/取图失败降级打开短时 signed photo；取消 share 返回 cancelled；`unit-photo-save.mjs` 已接入 `test:unit`，Domain Unit #167 PASS，H5 build job #569 PASS。既有 source→personal independent copy/delete DB 链未被本切片修改，沿用 affected-scope evidence | Browser/真机验证 iOS Safari/微信 WebView 两处单张保存、取消与降级路径；无需 Builder 继续改代码，除非 Browser 发现真实缺口 |
| V7-QUICK-02 | Unique legal draw regenerate UX | P1 | BROWSER_PENDING | canonical 要求两人单打/唯一合法对阵不显示 regenerate；current `EventPage` 仅在 confirmed Entry `active.length > 2` 时渲染“重新生成对阵”，两 Entry 隐藏 destructive no-op；`unit-quick-lifecycle.mjs` 已锁定该 source contract，`test:unit` 已包含该脚本；current head Domain Unit #175 PASS，H5 build job #577 PASS | Browser 阶段确认两 Entry 不显示、3+ Entry 仍显示 regenerate；低层无需 Builder 继续改代码，除非 Browser 发现真实缺口 |
| V7-HALL-01 | Hall/mobile | P1 | BROWSER_PENDING | filter/mobile containment 已实现并有 Unit evidence | Browser/iPhone/WeChat phase |
| V7-BRAND-01A | Canonical C logo consistency | P1 | VERIFIED | canonical C 识别已在 H5 `.brand-mark`、`public/brand-icon.svg` 与 `public/share-cover.svg` 对齐：sage 圆角方块 + off-white 开放 C；share cover 已移除旧三圆点 pseudo-logo，favicon/apple-touch-icon 均指向 `brand-icon.svg`；`unit-brand-assets.mjs` 已接入 `test:unit`，current head Domain Unit #175 PASS，H5 build job #577 PASS。Raster social preview/真实微信抓取属于独立 V7-BRAND-01B | Done；后续仅在影响品牌资产/`.brand-mark`/canonical 的 commit 下失效 |
| V7-BRAND-01B | Raster social preview / OG / WeChat | P1 | BROWSER_PENDING | affected-scope evidence 闭环：`predev`/`prebuild` 以 Node built-ins 生成 1200×1200 `public/share-cover.png`；OG/Twitter metadata 使用 PNG 并声明 image/png/尺寸；`unit-brand-assets.mjs` 实际生成并校验 PNG signature/dimensions/metadata。current head Domain Unit #184 PASS，H5 build job #586 PASS。canonical 明确要求 Candidate 真实微信抓取/缓存兼容验证，低层证据不能替代平台抓取 | Candidate Browser/真实微信验证分享卡片实际抓取 PNG、缓存与小尺寸识别；若平台失败再区分资产/抓取/缓存问题 |
| V7-VENUE-01A | Standard Event venue location foundation | P2 | INFRA_BLOCKED | affected-scope code/Unit 未发现产品缺口：provider-neutral schema/migration 包含 `venue_name/address/latitude/longitude/place_id/provider`，`save_event` create/edit 持久化并校验坐标成对/范围，旧 `venue`→`venue_name` 兼容；Standard form 仅主动展开后提供“使用当前位置”，Quick 不接 geolocation；`unit-venue-location.mjs` 已进入 `test:unit`，current head Domain Unit #184 PASS，H5 build job #586 PASS。但该切片包含 schema/RPC migration，必须有真实 DB replay/contract；current Integration #180 在 Supabase CLI setup 阶段失败，clean replay job #586 亦在 isolated local Supabase 启动失败，migration replay 均未执行 | Infra 恢复后 Verifier 跑 clean migration replay/save_event DB contract；当前不得因 build/静态 Unit 写 VERIFIED，也不得写 CODE_REOPEN |
| V7-VENUE-01B | Standard Event detail external-map handoff | P2 | BROWSER_PENDING | affected-scope 低层证据闭环：Standard Event 详情优先展示 `venue_name`、可选 `venue_address`；仅 `event_mode!='quick'` 且 `hasVenueCoordinates(e)` 为真时生成 `browserMapProvider.externalMapUrl()`，入口使用 `target="_blank" rel="noreferrer"`，详情页不调用 `getCurrentPosition`。`unit-venue-location.mjs` 已锁定上述 source contract；current head Domain Unit #187 PASS，H5 build job #589 PASS。无需 DB Integration 才能证明本“详情外部 handoff”子切片，底层 venue persistence 仍由 V7-VENUE-01A 单独 INFRA_BLOCKED | Browser 阶段确认 Standard Event 有坐标时入口可用、无坐标时隐藏、Quick 始终隐藏；若浏览器/外部地图行为异常再回开 |
| V7-VENUE-01C | Standard Event place search / map picker provider | P2 | EXTERNAL_BLOCKED | MapProvider 边界已建立；当前未配置真实地点搜索/地图选点 provider credential，未硬编码 Key | Controller 提供 provider/credential 后 Builder 接入；阻塞期间不占开发队列 |
| V7-DOC-01 | V7 canonical/document cleanup | P1 | IN_PROGRESS | Workboard 建立；需同步治理、README/root changelog，并审计过期文档 | Controller/Builder docs slice |
| V7-REPO-01A | Repo restore-bundle legacy audit | P2 | VERIFIED | affected-scope 引用链已复核：`scripts/restore-source.mjs` 明确循环读取 `bundle/chunk00..08.txt`，`.github/workflows/restore-readable-source.yml` 的 Restore deployment source 步骤直接执行该脚本；九个 chunk 当前均存在。依据清理安全线“仍被 workflow/build/deploy 等引用的文件不得因看起来旧而删除”，保留并分类 `LEGACY_REVIEW` 是正确结论，当前没有删除资格 | Done；若未来先移除 restore workflow/script 及其审计/追溯依赖，再重新创建删除评估切片 |
| V7-REPO-01B | Legacy layered CSS import audit | P2 | VERIFIED | independent affected-scope verification confirms `src/main.tsx` directly imports `styles.css`、`polish.css`、`v6.css`、`postdeploy.css`、`v7-mobile.css`; these files are therefore current runtime/build dependencies. Under the cleanup safety line, filename age alone is not deletion evidence; no CSS was deleted or rewritten in this slice, so no synthetic regression test is required | Done；仅当 import graph/构建入口后续改变时重新评估删除资格 |
| V7-REPO-01C | `source.bundle.b64` legacy audit | P2 | VERIFIED | independent reference-graph verification confirms `npm run pack` executes `scripts/pack-source.mjs`, which deliberately writes both `bundle/chunk00..08.txt` and `source.bundle.b64`; `scripts/restore-source.mjs` restores from the chunk files and does not read the monolithic artifact. Because `source.bundle.b64` remains part of an explicit packaging path and its release/audit trace value has not been disproved, conservative `LEGACY_REVIEW` retention is correct under the cleanup safety line | Done；只有先证明 packaging path 与 release/audit 追溯依赖均已移除，才重新创建删除评估切片 |
| V7-REPO-01D | AUD-015 specialist workflow/script legacy audit | P2 | VERIFIED | independent affected-scope verification confirms `.github/workflows/aud015-doubles-partner-blackbox.yml` remains an active `release-candidate` push + `workflow_dispatch` workflow and directly runs `qa/aud015-doubles-partner-blackbox.mjs`; executed migration `20260829161024_audit_backlog_source_of_truth.sql` preserves `AUD-20260829-015 / legacy-015-doubles-withdrawal` in the audit registry. These are live workflow/release and historical audit-trace dependencies, so the pair is not deletion-eligible under the cleanup safety line and should remain `LEGACY_REVIEW`; no runtime code/migration change or synthetic product regression is required for this reference audit | Done；仅当 workflow trigger/script 与历史审计追溯依赖先被正式移除/替代时，才重新创建删除评估切片 |
| V7-REPO-01E | Exploratory Browser workflow/script legacy audit | P2 | VERIFIED | independent affected-scope verification confirms `.github/workflows/exploratory-browser-blackbox.yml` remains an active `release-candidate`/`workflow_dispatch` workflow and directly runs `qa/exploratory-browser-blackbox.mjs`; specialized canonical `docs/EXPLORATORY_BLACKBOX_BASELINE.md` explicitly names this workflow/script pair, requires same-SHA exploratory evidence, and makes completed exploratory classification part of the pre-Gate contract. These assets therefore have current test/release/canonical dependencies and are not deletion-eligible under the cleanup safety line；retaining them as `LEGACY_REVIEW` is correct | Done；仅当 specialized canonical/Gate contract 与 workflow/script references are formally removed or replaced 时，才重新创建删除评估切片 |
| V7-REPO-01F | Candidate Browser workflow/scripts legacy audit | P2 | VERIFIED | independent affected-scope reference verification confirms `.github/workflows/candidate-browser-blackbox.yml` is still an active `release-candidate` push + `repository_dispatch(vercel.deployment.success)` + `workflow_dispatch` workflow and directly runs both `qa/candidate-browser-blackbox.mjs` and `qa/candidate-full-lifecycle.mjs`; both scripts exist at current head. `docs/BROWSER_BLACKBOX_BASELINE.md` and `docs/RELEASE_GOVERNANCE.md` explicitly name the same workflow/scripts as the exact-head Candidate Browser and pre-Gate evidence path. These assets therefore have current test/release/canonical dependencies and are not deletion-eligible; retain as `LEGACY_REVIEW` | Done；仅当 Candidate Browser/release canonical 与 workflow/script references 正式移除或替换后再重新评估删除资格 |
| V7-REPO-01G | Post-deploy backlog note legacy audit | P2 | VERIFIED | independent reference verification confirms `docs/AUDIT_BACKLOG_SNAPSHOT_POSTDEPLOY_NOTE.md` explicitly identifies itself as a repository synchronization note for remediation branch `feature/20260831-postdeploy-ui-quickstart-filter-qa`, while also stating that live AUD truth remains in Supabase. The referenced historical branch still exists and currently points to `c309dcbcb6336a5162ac02882eb569476b78a3f3` (`test: stabilize AUD-015 identity readiness`). Because the note retains a live repository→historical-remediation trace and its audit/remediation trace value has not been disproved, it does not meet the Workboard safe-deletion conditions; retain as `LEGACY_REVIEW` | Done；仅当该 historical remediation branch/reference 的追溯价值被正式替代或证明为零后，再重新创建删除评估切片 |
| V7-REPO-01H | PR #22 release-blocker control note legacy audit | P2 | VERIFIED | independent affected-scope verification confirms `docs/CONTROL_NOTE_RELEASE_BLOCKER_CLASSIFICATION.md` explicitly scopes itself to PR #22 / AUD-20260902-003 and preserves the historical remediation boundary for Candidate Freeze, exact-head evidence and Release Gate. PR #22 is still a resolvable historical Draft PR on `feature/20260831-postdeploy-ui-quickstart-filter-qa`, so the note retains a concrete repository→release-governance remediation trace even without current runtime imports. No evidence shows that trace value has been superseded or reduced to zero；retention as `LEGACY_REVIEW` is therefore correct under the cleanup safety line | Done；仅当 PR #22/AUD-20260902-003 的治理追溯被正式迁移/替代并证明本 note 不再承担审计解释时，再重新创建删除评估切片 |
| V7-REPO-01I | Audit backlog degraded snapshot legacy audit | P2 | VERIFIED | independent reference-chain verification confirms `docs/AUDIT_BACKLOG_SNAPSHOT.json` self-identifies as a read-only fallback while Supabase remains sole truth；README and `docs/ENVIRONMENT_BASELINE.md` still name it as degraded cache，and `docs/AUDIT_AUTOMATION_GOVERNANCE.md` explicitly requires reading it when all three formal backlog read paths are unavailable，with strict stale/read-only limits. This is a current automation/canonical dependency，so the snapshot is not deletion-eligible and retention as `LEGACY_REVIEW` is correct | Done；只有先从 automation/canonical degraded-mode contract 中正式移除该 snapshot 并提供替代路径，才重新创建删除评估切片 |
| V7-REPO-01J | Post-deploy remediation scope legacy audit | P2 | VERIFIED | independent affected-scope verification confirms `docs/POSTDEPLOY_20260831_FIX_SCOPE.md` explicitly identifies itself as the remediation record after the first main/Production release and explicitly says it is not a new product baseline. It names both `20260831102924_postdeploy_quickstart_level_filter_qa_isolation.sql` and `20260831103222_broaden_qa_event_hall_isolation.sql`; both migration files remain present at the verification head. This preserves a concrete historical remediation→executed migration trace，so the document is not deletion-eligible and retention as `LEGACY_REVIEW` is correct | Done；仅当该 first-Production remediation/migration trace 被正式迁移或替代并证明本文件不再承担追溯价值时，再重新创建删除评估切片 |
| V7-REPO-01K | H5 Build workflow legacy audit | P2 | VERIFIED | independent affected-scope verification confirms `.github/workflows/build.yml` is an active `H5 Build Check` triggered by both `pull_request`→`main` and `push`→`main`. It performs repository visibility/environment/secret preflights，`npm ci` + `npm run build`，migration filename/parity checks，isolated local Supabase clean replay，and schema/RPC assertions. This is a current build/test/release-safety dependency，so the workflow is not deletion-eligible and retention as `LEGACY_REVIEW` is correct | Done；仅当现行 build/test/release safety contract 被正式替代且所有 trigger/check 迁移完成后，再重新创建删除评估切片 |
| V7-REPO-01L | Integration Contract workflow legacy audit | P2 | VERIFIED | independent affected-scope reference verification confirms `.github/workflows/integration-contract-tests.yml` is active on `pull_request`→`main` and `push`→`main`，performs migration-parity checks，starts isolated local Supabase，replays repository migrations from a clean database，and then executes `npm run test:integration`. Current `package.json` maps `test:integration` to the scoring/event lifecycle/Quick/photo/privacy integration chain. This is a current test/migration-safety dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. This reference-only audit changes no runtime code/migration/canonical AC，so actual DB execution is not required to establish the workflow→package-script dependency itself | Done；仅当 Integration Contract workflow/test:integration contract 被正式替代且所有 migration/test-safety responsibilities 完整迁移后，再重新创建删除评估切片 |
| V7-REPO-01M | Domain Unit workflow legacy audit | P2 | VERIFIED | independent affected-scope reference verification confirms `.github/workflows/unit-tests.yml` is active on `pull_request`→`main` and `push`→`main`；after checkout/setup-node/npm ci it directly executes `npm run test:unit`. Current `package.json` maps `test:unit` to the domain/scoring/Hall/mobile/Quick/Identity/Photo/Brand/Venue regression chain. This is a current test-safety dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. No runtime code/migration/canonical AC changed in this reference-only slice | Done；仅当 Domain Unit workflow/test:unit contract 被正式替代且全部 unit regression responsibilities 完整迁移后，再重新创建删除评估切片 |
| V7-REPO-01N | V6 release changelog legacy audit | P2 | VERIFIED | independent reference verification confirms `docs/CHANGELOG_20260829_V6.md` explicitly self-identifies as a 2026-08-29 historical snapshot rather than current truth, preserves the then-current branch/PR #20 state, product/data-compat changes, documentation lineage and verification focus. `docs/DOCUMENT_GOVERNANCE.md` explicitly classifies changelog/CI/PR material as evidence of what happened at a point in time and historical snapshots as retained reference that must not override current canonical policy. The file therefore still carries concrete release/audit trace value; retention as `LEGACY_REVIEW` is correct and no runtime Unit/Integration evidence is required for this reference-only classification | Done；仅当 V6 release history 已被正式迁移到等价不可丢失的追溯载体并证明本文件不再承担历史解释时，再重新创建删除评估切片 |
| V7-REPO-01O | V7 work-split coordination note legacy audit | P2 | VERIFIED | independent reference verification confirms `docs/NEXT_VERSION_WORK_SPLIT_20260902.md` explicitly scopes itself to Draft PR #24 / `feature/20260902-next-version-scoring-quick-photo` and records the original Scoring-vs-Product ownership split, exact-head incremental-write discipline, no-force/no-reset rule, implementer-not-VERIFIED boundary and pre-release prohibitions. PR #24 remains open and Draft on that same branch, while `docs/DOCUMENT_GOVERNANCE.md` reserves current product truth for its canonical set and treats historical/evidence material as non-canonical trace rather than disposable content. This note therefore retains a concrete coordination/governance trace for the still-open PR and is correctly retained as `LEGACY_REVIEW`; no product/runtime test is needed for this reference-only classification | Done；当 PR #24 生命周期结束且该 coordination/governance trace 已被正式迁移或证明无后续审计价值时，再重新创建删除评估切片 |
| V7-REPO-01P | V6 PRD canonical retention audit | P2 | VERIFIED | independent reference verification confirms `docs/PRD_V6_EVENT_LIFECYCLE_PRIVACY_I18N.md` still defines V6 incremental product rules, while current `docs/DOCUMENT_GOVERNANCE.md` explicitly lists that file in the Canonical policy / product baseline set and states that only rules explicitly changed by `NEXT_VERSION_PRODUCT_BASELINE_20260902.md` are superseded. The file is therefore a current canonical dependency, not deletion-eligible merely because its filename says V6；retention as `LEGACY_REVIEW` is correct. This reference-only classification changes no runtime code/migration/product AC, so synthetic Unit/Integration evidence is not required | Done；仅当 document governance 正式移除/替代该 canonical dependency 并完成规则迁移后，再重新创建删除评估切片 |
| V7-REPO-01Q | V7 development changelog retention audit | P2 | VERIFIED | independent reference verification confirms `docs/CHANGELOG_20260902_V7.md` explicitly records current V7 development state while disclaiming product-source-of-truth status；current `docs/DOCUMENT_GOVERNANCE.md` classifies CHANGELOG as Evidence / changelog and requires all observable behavior changes to be written to CHANGELOG. The file therefore remains an active development evidence/trace dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. This reference-only classification changes no runtime code/migration/product AC, so synthetic Unit/Integration evidence is not required | Done；仅当 V7 development evidence has been formally migrated/replaced and governance no longer depends on this changelog，再重新创建删除评估切片 |
| V7-REPO-01R | Unit Test baseline retention audit | P2 | VERIFIED | independent current-head reference verification confirms `docs/UNIT_TEST_BASELINE.md` explicitly self-identifies as the current Unit/white-box specialist canonical baseline, defines `npm run test:unit` and `.github/workflows/unit-tests.yml` as the active execution path, and current `docs/INTEGRATION_TEST_BASELINE.md` references it as layer 1 of the four-layer test structure. The workflow itself remains active on PR→main and main push and executes `npm run test:unit`. This is a current canonical/test dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. This reference-only classification changes no runtime code/migration/product AC, so DB Integration or synthetic product regression is not required | Done；仅当 Unit canonical/testing contract 被正式迁移或替代并移除所有引用后，再重新创建删除评估切片 |
| V7-REPO-01S | Brand asset baseline retention audit | P2 | VERIFIED | independent current-head canonical reference verification confirms `docs/BRAND_ASSET_BASELINE.md` explicitly declares itself the specialist canonical source for Logo、brand icon 与 social share cover, while current `docs/DOCUMENT_GOVERNANCE.md` lists it in the Canonical policy / product baseline set. It therefore remains a current canonical dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. This reference-only classification changes no runtime code/migration/product AC, so Unit/Integration evidence is not required | Done；仅当 document governance 正式迁移/替代该品牌 canonical 并移除当前引用后，再重新创建删除评估切片 |
| V7-REPO-01T | Root CHANGELOG retention audit | P2 | VERIFIED | independent current-head reference verification confirms root `CHANGELOG.md` explicitly records product behavior、data-model、permission、architecture 与 release changes while disclaiming current product-source-of-truth status；current `docs/DOCUMENT_GOVERNANCE.md` classifies CHANGELOG as Evidence / changelog and requires all observable behavior changes to be written to CHANGELOG. The root changelog therefore remains a current evidence/release-trace dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. This reference-only classification changes no runtime code/migration/canonical AC, so synthetic Unit/Integration evidence is not required | Done；仅当 changelog/evidence responsibility 被正式迁移或替代并证明 root CHANGELOG 不再承担 release trace 时，再重新创建删除评估切片 |
| V7-REPO-01U | Audit automation governance retention audit | P2 | VERIFIED | independent current-head canonical reference verification confirms `docs/AUDIT_AUTOMATION_GOVERNANCE.md` explicitly defines the long-term automation audit/remediation governance baseline and delegates release/browser details to current specialized baselines；current `docs/DOCUMENT_GOVERNANCE.md` explicitly lists it in the Canonical policy / product baseline set. It therefore remains a current canonical/governance dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. This reference-only classification changes no runtime code/migration/product AC, so synthetic Unit/Integration evidence is not required | Done；仅当 document governance 正式迁移/替代该 automation governance canonical 并移除当前依赖后，再重新创建删除评估切片 |
| V7-REPO-01V | Browser Blackbox baseline retention audit | P2 | VERIFIED | independent current-head canonical/reference verification confirms `docs/BROWSER_BLACKBOX_BASELINE.md` explicitly self-identifies as the canonical source for Candidate Browser Blackbox evidence, directly names `.github/workflows/candidate-browser-blackbox.yml` plus `qa/candidate-browser-blackbox.mjs` / `qa/candidate-full-lifecycle.mjs`, and the workflow remains active on release-candidate / deployment / manual triggers. Current `docs/DOCUMENT_GOVERNANCE.md` explicitly lists the Browser Blackbox baseline in the Canonical policy / product baseline set. This is therefore a current canonical/test/release dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. Reference-only classification changes no runtime code/migration/product AC, so synthetic Unit/Integration evidence is not required | Done；仅当 Browser Blackbox canonical/testing contract 被正式迁移或替代并移除 workflow/script references 后，再重新创建删除评估切片 |
| V7-REPO-01W | Release Governance baseline retention audit | P2 | VERIFIED | independent current-head canonical/reference verification confirms `docs/RELEASE_GOVERNANCE.md` explicitly self-identifies as the canonical source for release branches、Candidate Freeze、Vercel Preview、Browser Blackbox、Release Gate 与 main/CloudBase sequencing, and current `docs/DOCUMENT_GOVERNANCE.md` explicitly lists it in the Canonical policy / product baseline set. The release baseline also directly cross-references `docs/BROWSER_BLACKBOX_BASELINE.md` and the active Candidate Browser workflow/scripts. It is therefore a current canonical/release dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. Reference-only classification changes no runtime code/migration/product AC, so synthetic Unit/Integration evidence is not required | Done；仅当 release governance contract 被正式迁移或替代并完成所有发布/Browser/Gate 依赖迁移后，再重新创建删除评估切片 |
| V7-REPO-01X | Integration Test baseline retention audit | P2 | VERIFIED | independent current-head reference verification confirms `docs/INTEGRATION_TEST_BASELINE.md` explicitly self-identifies as the canonical Integration/Contract testing baseline, defines the Unit→Integration→E2E→Gate layering, and names `npm run test:integration` plus GitHub Actions `Integration Contract Tests` as the active execution path. Current `.github/workflows/integration-contract-tests.yml` still performs migration parity, isolated local Supabase startup/clean replay and then executes `npm run test:integration`; `docs/DOCUMENT_GOVERNANCE.md` still requires Integration synchronization for Scoring and Storage/RLS changes. This is a current canonical/test/migration-safety dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. Reference-only classification changes no runtime code/migration/product AC, so synthetic DB execution is not required to prove the dependency chain itself | Done；仅当 Integration canonical/testing contract 被正式迁移或替代并完成 workflow/contract responsibilities 迁移后，再重新创建删除评估切片 |
| V7-REPO-01Y | Environment baseline retention audit | P2 | VERIFIED | independent current-head canonical/reference verification confirms `docs/ENVIRONMENT_BASELINE.md` explicitly self-identifies as the canonical source for repository/environment identity、branch/deployment boundaries、Supabase migration rules、frontend runtime configuration、Vercel candidate semantics and audit fallback, and current `docs/DOCUMENT_GOVERNANCE.md` explicitly lists it in the Canonical policy / product baseline set. The environment baseline also directly delegates release/browser sequencing to the current specialized baselines and requires runtime revalidation of dynamic facts. It is therefore a current canonical/environment/release-safety dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. Reference-only classification changes no runtime code/migration/product AC, so synthetic Unit/Integration evidence is not required | Done；仅当 environment governance responsibilities 被正式迁移/替代并移除所有 current canonical/release dependencies 后，再重新创建删除评估切片 |
| V7-REPO-01Z | Exploratory Blackbox baseline retention audit | P2 | VERIFIED | independent current-head reference verification confirms `docs/EXPLORATORY_BLACKBOX_BASELINE.md` explicitly self-identifies as the specialized canonical baseline for Exploratory Browser Blackbox, requires same exact-head Preview evidence, non-fail-fast independent scenario collection and pre-Gate finding classification/root-cause. Current `.github/workflows/exploratory-browser-blackbox.yml` remains active on `release-candidate` push/manual dispatch, verifies exact candidate SHA + Vercel build metadata and directly runs `qa/exploratory-browser-blackbox.mjs`. This is therefore a current canonical/test/release dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. Reference-only classification changes no runtime code/migration/product AC, so synthetic Unit/Integration evidence is not required | Done；仅当 exploratory canonical/Gate contract 与 workflow/script references 被正式迁移或替代后再重新评估删除资格 |
| V7-REPO-01AB | Event Lifecycle baseline retention audit | P2 | VERIFIED | independent current-head canonical/reference verification confirms `docs/EVENT_LIFECYCLE_BASELINE.md` explicitly self-identifies as the Event lifecycle specialist canonical baseline; current `docs/DOCUMENT_GOVERNANCE.md` lists it in the Canonical policy / product baseline set and its linkage matrix requires lifecycle changes to synchronize EVENT_LIFECYCLE. This is a current canonical/product dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. Reference-only classification changes no runtime code/migration/product AC, so synthetic Unit/Integration evidence is not required | Done；仅当 Event lifecycle canonical responsibility 被正式迁移/替代并从 document governance/current product dependency 中移除后，再重新创建删除评估切片 |
| V7-REPO-01AC | Document Governance baseline retention audit | P2 | VERIFIED | independent current-head verification confirms `docs/DOCUMENT_GOVERNANCE.md` explicitly declares itself the long-term documentation-governance canonical source, defines canonical/runtime/evidence/historical precedence and conflict resolution, lists itself in the current Canonical policy / product baseline set, and requires development/test/audit consumers to read the current-stage baseline before affected work. Deleting it would remove the repository's source-of-truth/conflict-resolution contract；retention as `LEGACY_REVIEW` is therefore correct. Reference-only classification changes no runtime code/migration/product AC, so synthetic Unit/Integration evidence is not required | Done；仅当 documentation-governance responsibilities are formally migrated/replaced and all consumers no longer depend on this file，再重新创建删除评估切片 |
| V7-REPO-01AD | Interaction baseline retention audit | P2 | VERIFIED | independent current-head canonical/reference verification confirms `docs/INTERACTION_BASELINE.md` explicitly defines current H5 MVP IA/UX constraints and delegates Photo/Quick/Lifecycle/Tournament specialties to their canonical baselines；current `docs/DOCUMENT_GOVERNANCE.md` explicitly lists `docs/INTERACTION_BASELINE.md` in the Canonical policy / product baseline set and requires it to synchronize Scoring、Quick Start、Photo、Hall/mobile、Lifecycle and Tournament changes. It is therefore a current canonical/product dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. Reference-only classification changes no runtime code/RPC/DB/migration/product AC, so synthetic Unit/Integration evidence is not required | Done；仅当 Interaction canonical responsibilities 被正式迁移/替代并从 document governance/current product dependency 中移除后，再重新创建删除评估切片 |
| V7-REPO-01AE | Next-version product baseline retention audit | P2 | VERIFIED | independent current-head canonical verification confirms `docs/NEXT_VERSION_PRODUCT_BASELINE_20260902.md` is explicitly `APPROVED FOR DEVELOPMENT`, defines the V7 approved Scoring/Quick/Photo/Hall/Identity/Standard Event venue changes, and states that its explicit changes override older descriptions while unchanged rules continue from existing canonicals. Current `docs/DOCUMENT_GOVERNANCE.md` lists it first in the Canonical policy / product baseline set and gives it precedence for approved next-version rules. It is therefore an active current product dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. Reference-only classification changes no runtime code/migration/product AC, so synthetic Unit/Integration evidence is not required | Done；仅当 next-version approved product rules are formally migrated/replaced and document governance no longer treats this file as current precedence，再重新创建删除评估切片 |
| V7-REPO-01AF | Product baseline retention audit | P2 | VERIFIED | independent current-head canonical/reference verification confirms `docs/PRODUCT_BASELINE.md` explicitly self-identifies as a current H5 MVP product truth source and carries active Event/Quick/Hall/identity/privacy/lifecycle rules. Current `docs/DOCUMENT_GOVERNANCE.md` explicitly lists `docs/PRODUCT_BASELINE.md` in the Canonical policy / product baseline set and requires PRODUCT synchronization across Scoring、Quick、Photo、Hall/mobile、Lifecycle and Tournament changes. It is therefore a current canonical/product dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. Reference-only classification changes no runtime code/RPC/DB/migration/product AC, so synthetic Unit/Integration evidence is not required | Done；仅当 PRODUCT canonical responsibilities 被正式迁移/替代并从 current governance/read chain 中移除后，再重新创建删除评估切片 |
| V7-REPO-01AG | P0 Acceptance baseline retention audit | P2 | VERIFIED | independent current-head canonical/reference verification confirms `docs/P0_ACCEPTANCE.md` explicitly self-identifies as the current long-term P0 acceptance baseline and delegates active specialist truth to Photo/Quick/Lifecycle/Tournament/Release/Browser canonicals. Current `docs/DOCUMENT_GOVERNANCE.md` explicitly lists `docs/P0_ACCEPTANCE.md` in the Canonical policy / product baseline set, while README lists it in the canonical-source read chain. It is therefore a current canonical/acceptance dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. Reference-only classification changes no runtime code/RPC/DB/migration/product AC, so synthetic Unit/Integration evidence is not required | Done；仅当 P0 acceptance responsibilities/read-chain 被正式迁移或替代并移除 current canonical references 后，再重新创建删除评估切片 |
| V7-REPO-01AH | Photo Album baseline retention audit | P2 | VERIFIED | independent current-head canonical/reference verification confirms `docs/PHOTO_ALBUM_BASELINE.md` explicitly self-identifies as the current Photo specialist product truth and carries active source/personal-copy lifecycle, privacy and authorization rules. Current `docs/DOCUMENT_GOVERNANCE.md` explicitly lists it in the Canonical policy / product baseline set and requires PHOTO_ALBUM synchronization for Photo changes；README also lists it in the canonical read chain and summarizes the same independent personal-copy model. It is therefore a current canonical/product/security dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. Reference-only classification changes no runtime code/RPC/DB/migration/product AC, so synthetic Unit/Integration evidence is not required | Done；仅当 Photo canonical responsibilities 被正式迁移/替代并从 current governance/read chain 中移除后，再重新创建删除评估切片 |
| V7-REPO-01AI | Quick Start baseline retention audit | P2 | VERIFIED | independent current-head canonical/reference verification confirms `docs/QUICK_START_BASELINE.md` explicitly self-identifies as the Canonical product baseline for Quick Start and carries active participant、lifecycle、scoring、single-match、Hall/test-isolation and identity rules. Current `docs/DOCUMENT_GOVERNANCE.md` explicitly lists it in the Canonical policy / product baseline set and requires QUICK_START synchronization for Quick changes；README also names it as the Quick Start specialist truth in the canonical read chain. It is therefore a current canonical/product dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. Reference-only classification changes no runtime code/RPC/DB/migration/product AC, so synthetic Unit/Integration evidence is not required | Done；仅当 Quick Start canonical responsibilities 被正式迁移/替代并从 current governance/read chain 中移除后，再重新创建删除评估切片 |
| V7-REPO-01AJ | User Story acceptance baseline retention audit | P2 | VERIFIED | independent current-head canonical/reference verification confirms `docs/USER_STORY_ACCEPTANCE_BASELINE.md` explicitly self-identifies as the User Story / Acceptance Criteria truth for blackbox testing and version acceptance, requires release-candidate scenarios to read it with specialist canonicals, and carries active role/state/mobile/recovery AC. Current `docs/DOCUMENT_GOVERNANCE.md` lists it in the Canonical policy / product baseline set and requires USER_STORY synchronization for Scoring、Quick、Photo、Hall/mobile、Lifecycle and Tournament changes；README also lists it in the canonical-source read chain. It is therefore a current canonical/acceptance/test dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. Reference-only classification changes no runtime code/RPC/DB/migration/product AC, so synthetic Unit/Integration execution is not required | Done；仅当 User Story/Acceptance canonical responsibilities 被正式迁移或替代并从 current governance/read chain 中移除后，再重新创建删除评估切片 |
| V7-REPO-01AK | Test Data Governance baseline retention audit | P2 | VERIFIED | independent current-head canonical/reference verification confirms `docs/TEST_DATA_GOVERNANCE.md` explicitly declares itself a canonical governance baseline for Candidate Browser、Exploratory Browser、AUD specialist browser tests and future E2E/Smoke/Regression automation, and defines active `TST-*` identity naming、Hall isolation、legacy compatibility and cleanup rules. Current README names it as the automated-test identity/naming/isolation specialist truth；`docs/DOCUMENT_GOVERNANCE.md` lists it in the canonical policy/product baseline set and delegates automated test data naming/isolation to it. It is therefore a current canonical/test-governance dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. Reference-only classification changes no runtime code/RPC/DB/migration/product AC, so synthetic Unit/Integration execution is not required | Done；仅当 automated-test data governance responsibilities 被正式迁移或替代并从 current governance/read chain 中移除后，再重新创建删除评估切片 |
| V7-REPO-01AL | Visual Design baseline retention audit | P2 | VERIFIED | independent current-head canonical/reference verification confirms `docs/VISUAL_DESIGN_BASELINE.md` explicitly marks itself as long-term frontend design constraint and the default H5 page/component visual implementation basis. Current `docs/DOCUMENT_GOVERNANCE.md` explicitly lists it in the Canonical policy / product baseline set and requires VISUAL synchronization for Hall/mobile filter changes. It therefore remains a current canonical/UI dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. This reference-only classification changes no runtime code/RPC/DB/migration/product AC, so synthetic Unit/Integration execution is not required | Done；仅当 Visual canonical responsibilities 被正式迁移/替代并从 current governance/read chain 中移除后，再重新创建删除评估切片 |
| V7-REPO-01AM | Tournament Presentation baseline retention audit | P2 | VERIFIED | independent current-head canonical/reference verification confirms `docs/TOURNAMENT_PRESENTATION_BASELINE.md` explicitly declares itself the canonical product/interaction baseline for Standard/Quick draw, tennis scoring hierarchy, rounds, match/result presentation and live scoring. Current `docs/DOCUMENT_GOVERNANCE.md` explicitly lists it in the Canonical policy / product baseline set and requires TOURNAMENT_PRESENTATION synchronization for Scoring and draw/round changes. It therefore remains a current canonical/product/scoring dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. This reference-only classification changes no runtime code/RPC/DB/migration/product AC, so synthetic Unit/Integration execution is not required | Done；仅当 Tournament Presentation canonical responsibilities 被正式迁移/替代并从 current governance/read chain 中移除后，再重新创建删除评估切片 |
| V7-REPO-01AN | V7 Workboard retention audit | P2 | VERIFIED | independent current-head verification confirms `docs/V7_WORKBOARD.md` explicitly defines itself as the machine-consumable truth for current V7 work state and records queue ownership/blockers, and this PR #24 development flow is actively consuming that queue as its control plane. Deleting or relocating it would break the active development-state source. Retention as `LEGACY_REVIEW` is therefore correct. This reference-only classification changes no runtime code/RPC/DB/migration/product AC, so synthetic Unit/Integration evidence is not required | Done；仅当 V7 development control-plane responsibility 被正式迁移/替代且所有 active consumers no longer depend on this path 后，再重新创建删除评估切片 |
| V7-REPO-01AO | Release candidate deployment marker retention audit | P2 | VERIFIED | independent current-head verification confirms `public/release-candidate-marker.txt` explicitly states its purpose is to make QA-only/backend-only release-head changes count as Vite/Vercel application build input so exact-head Preview evidence can be generated. Current `docs/RELEASE_GOVERNANCE.md` still requires Candidate Freeze to produce an exact-head `release-candidate` Vercel Preview whose deployment metadata and `/build-meta.json` match the PR exact head. No current evidence establishes an equivalent replacement trigger or proves this marker safe to remove; absence of a stronger textual import alone is not deletion evidence. Retention as `LEGACY_REVIEW` is therefore correct. This reference-only classification changes no runtime code/RPC/DB/migration/product AC, so synthetic Unit/Integration evidence is not required | Done；仅当 exact-head candidate deployment trigger semantics 被正式迁移到等价机制并验证 marker 不再承担 build-input/release-trace 责任后，再重新创建删除评估切片 |
| V7-REPO-01AP | Root README retention audit | P2 | VERIFIED | independent current-head reference verification confirms root `README.md` is the repository's active canonical read-chain/navigation entry: it explicitly requires larger development, audit and Release Gate readers to load current governance/product baselines, summarizes active Quick/Photo/Environment/Release/Audit contracts, and points to current source/migration/Edge/CI evidence. Removing it would break the documented current read path and operational entrypoint；retention as `LEGACY_REVIEW` is correct. This reference-only classification changes no runtime code/RPC/DB/migration/product AC, so synthetic Unit/Integration execution is not required | Done；仅当 repository navigation/governance read-chain responsibility 被正式迁移/替代并移除 current consumers 后，再重新创建删除评估切片 |
| V7-REPO-01AQ | Environment example retention audit | P2 | VERIFIED | independent current-head reference verification confirms root `.env.example` carries the canonical frontend Supabase URL plus publishable-key placeholder, and current `.github/workflows/build.yml` directly requires the exact canonical URL from `.env.example` in the `Validate canonical environment identity` preflight before `npm ci` / `npm run build`. It is therefore a current build/environment-safety dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. This reference-only classification changes no runtime code/RPC/DB/migration/product AC, so build PASS or DB Integration is not required to prove this direct dependency chain | Done；仅当 build/environment identity preflight is formally migrated to an equivalent source and `.env.example` is no longer consumed，再重新创建删除评估切片 |
| V7-REPO-01AR | Vercel ignore retention audit | P2 | VERIFIED | independent current-head reference verification confirms root `.vercelignore` currently excludes `node_modules`、`.env*`（并显式保留 `.env.example`）、`.server-build`、test/playwright reports、`e2e`、`src/tests` 与 `docs/requirements`. Current `docs/ENVIRONMENT_BASELINE.md` defines Vercel as the active public Preview/candidate deployment path and treats deployment configuration as governed environment behavior. Because `.vercelignore` directly controls current Vercel deployment packaging and no equivalent replacement is present, it is a current deployment-safety dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. This reference-only classification changes no runtime code/RPC/DB/migration/product AC, so synthetic Unit/Integration evidence is not required | Done；仅当 Vercel deployment packaging/ignore responsibility 被正式迁移到等价机制并验证该文件不再被当前部署路径消费后，再重新创建删除评估切片 |
| V7-REPO-01AS | Git ignore retention audit | P2 | VERIFIED | independent current-head reference verification confirms root `.gitignore` excludes current generated/developer-local paths including `node_modules/`、`dist/`、`.env*`（并显式保留 `.env.example`）、`.vercel/`、test/coverage/report artifacts、`.server-build/` 与 generated `public/share-cover.png`. Current `package.json` actively runs Vite dev/build/test flows and invokes `scripts/generate-share-cover-png.mjs` in both `predev` and `prebuild`, so these ignore entries map to active build/test/local-secret/generated outputs rather than dead names. Removing `.gitignore` would change current repository tracking hygiene and secret/generated-artifact boundaries; no equivalent replacement is present. Retention as `LEGACY_REVIEW` is therefore correct. This reference-only classification changes no runtime code/RPC/DB/migration/product AC, so synthetic Unit/Integration evidence is not required | Done；仅当 repository tracking/secret/generated-artifact hygiene responsibility 被正式迁移到等价机制且这些 current paths 不再依赖该 ignore file 后，再重新创建删除评估切片 |
| V7-REPO-01AT | NPM package lock retention audit | P2 | VERIFIED | independent current-head reference verification confirms root `package-lock.json` is `lockfileVersion: 3` and pins the active dependency graph. Current H5 Build and Domain Unit workflows both configure `actions/setup-node` with `cache: npm` and then execute `npm ci`; `npm ci` is therefore part of the active reproducible-install contract backed by the current lockfile, not a dead generated artifact. No equivalent lock mechanism is present. Retention as `LEGACY_REVIEW` is correct. This reference-only classification changes no runtime code/RPC/DB/migration/product AC, so synthetic Unit/Integration execution is not required | Done；仅当 current npm install/cache contract 被正式迁移到等价 lock mechanism 且 workflows 不再消费该 lockfile 后，再重新创建删除评估切片 |
| V7-REPO-01AU | NPM package manifest retention audit | P2 | VERIFIED | independent current-head reference verification confirms root `package.json` defines the active `dev`、`test:unit`、`test:integration`、`build`、`pack` and `restore` script contracts plus runtime/dev dependency versions. Current H5 Build workflow executes `npm run build` after `npm ci`; Domain Unit executes `npm run test:unit`; Integration Contract executes `npm run test:integration`. The manifest is therefore a direct current build/test/runtime-tooling dependency rather than a dead legacy artifact, with no equivalent manifest or execution entrypoint present. Retention as `LEGACY_REVIEW` is correct. This reference-only classification changes no runtime code/RPC/DB/migration/product AC, so synthetic Unit/Integration execution is not required | Done；仅当 Node/Vite build/test/tooling execution contracts 被正式迁移到等价 manifest/entrypoint 且 current workflows no longer consume root `package.json` 后，再重新创建删除评估切片 |
| V7-REPO-01AV | Vite config retention audit | P2 | VERIFIED | independent current-head reference verification confirms root `vite.config.ts` imports Vite `defineConfig` plus `@vitejs/plugin-react`, registers the React plugin, binds the dev server host and sets build target `es2022`. Current `package.json` directly maps `dev`/`build`/`preview` to Vite commands and declares both `vite` and `@vitejs/plugin-react`, while the current root tree contains `vite.config.ts` as the Vite config. This is a direct current dev/build configuration dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. This reference-only classification changes no runtime code/RPC/DB/migration/canonical AC, so synthetic Unit/Integration execution is not required | Done；仅当 Vite execution/config discovery contract 被正式迁移或替代且 current commands no longer consume this config 后，再重新创建删除评估切片 |
| V7-REPO-01AW | Root HTML entrypoint retention audit | P2 | VERIFIED | independent current-head reference verification confirms root `index.html` is the active Vite H5 document entrypoint: it provides `#root`, mounts `/src/main.tsx`, loads `/storage-compat.js`, declares `/brand-icon.svg` favicon/apple-touch icon and `/share-cover.png` OG/Twitter metadata. Current `src/main.tsx` performs `ReactDOM.createRoot(document.getElementById("root")!)` and renders the application, while `package.json` builds/serves through Vite. Removing `index.html` would therefore break the current application boot document and social/brand metadata path；retention as `LEGACY_REVIEW` is correct. This reference-only classification changes no runtime code/RPC/DB/migration/canonical AC, so synthetic Unit/Integration execution is not required | Done；仅当 Vite HTML entry/runtime asset responsibility 被正式迁移到 an equivalent entrypoint and current boot/metadata references are removed 后，再重新创建删除评估切片 |
| V7-REPO-01AX | Storage compatibility bootstrap retention audit | P2 | VERIFIED | independent current-head reference verification confirms root `index.html` loads `/storage-compat.js` before the ESM `/src/main.tsx` entry. Current `public/storage-compat.js` explicitly bootstraps a stable `navigator.storage` facade for missing/partial WebKit StorageManager surfaces, preserves native methods when complete, and documents pre-ESM consumers including Supabase Auth. This is therefore a live application-boot/runtime-compat dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. This reference-only classification changes no runtime code/RPC/DB/migration/canonical AC, so synthetic Unit/Integration evidence is not required | Done；仅当 pre-ESM storage compatibility responsibility 被正式迁移/替代且 `index.html` 不再加载该 bootstrap 后，再重新创建删除评估切片 |
| V7-REPO-01AY | TypeScript project config retention audit | P2 | VERIFIED | independent current-head reference verification confirms root `package.json` executes `tsc --noEmit` before `vite build`, while root `tsconfig.json` defines the active ES2022/DOM、Bundler module resolution、React JSX、strict checking、Vite client types and includes current `src` plus `vite.config.ts`. Removing it would change/break the active TypeScript compile contract and no equivalent project config is present；retention as `LEGACY_REVIEW` is correct. This reference-only classification changes no runtime code/RPC/DB/migration/canonical AC, so synthetic Unit/Integration execution is not required | Done；仅当 TypeScript compile/config responsibility 被正式迁移到等价 project config and current build no longer consumes root `tsconfig.json` 后，再重新创建删除评估切片 |
| V7-REPO-01AZ | Server TypeScript config retention audit | P2 | VERIFIED | independent current-head reference verification confirms root `tsconfig.server.json` targets NodeNext and includes only `api/**/*.ts`, while the exact-head recursive tree contains no `api/` path. Current `package.json` has no server TypeScript project command and builds through `tsc --noEmit && vite build`; current `vercel.json` delegates to `npm run build`, outputs `dist`, and rewrites to the SPA entry, with no `tsconfig.server.json`/`api` consumer. No active runtime/build/test/deploy dependency is established. However `.gitignore` / `.vercelignore` still preserve `.server-build`, and no evidence proves historical release/audit trace dependence is zero. Under the conservative cleanup rule, retention as `LEGACY_REVIEW` is therefore correct rather than deletion. This reference-only classification changes no runtime code/RPC/DB/migration/canonical AC, so synthetic Unit/Integration evidence is not required | Done；仅当 historical release/audit trace is formally migrated/replaced or proven zero and no current consumer emerges，再重新创建删除评估切片 |
| V7-REPO-01BA | Vercel project config retention audit | P2 | VERIFIED | independent current-head canonical/reference verification confirms root `vercel.json` remains the active deployment contract: `framework=vite`, `buildCommand=npm run build`, `outputDirectory=dist`, Git deployment disabled by default with only `main` and `release-candidate` enabled, SPA rewrite to `/index.html`, and baseline security headers. Current `docs/ENVIRONMENT_BASELINE.md` independently defines the same Vercel build/output values and candidate-branch whitelist as canonical environment behavior. This is a current deployment/release-safety dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. This reference-only classification changes no runtime code/RPC/DB/migration/product AC, so synthetic Unit/Integration evidence is not required | Done；仅当 Vercel deployment/config responsibility is formally migrated to an equivalent mechanism and current Environment canonical no longer depends on this file，再重新创建删除评估切片 |
| V7-REPO-01BB | Live migration manifest append retention audit | P2 | VERIFIED | independent current-head reference verification confirms `.github/workflows/build.yml` invokes `node scripts/check-migration-parity.mjs --allow-repo-only` for PR clean-replay preflight and exact parity on main before isolated local Supabase startup/replay. `scripts/check-migration-parity.mjs` conditionally reads `supabase/LIVE_MIGRATION_MANIFEST_APPEND.json` when present and merges its `sha256_by_version` into the expected live migration set; the current append manifest contains seven SHA-256 version entries. It is therefore a current build/migration-safety dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. This reference-only classification changes no runtime code/RPC/DB/migration/canonical AC, so synthetic Unit/Integration execution is not required | Done；仅当 migration-parity/build safety responsibility 被正式迁移或替代且 parity script no longer consumes the append manifest 后，再重新创建删除评估切片 |
| V7-REPO-01BC | Live migration manifest retention audit | P2 | VERIFIED | independent current-head reference verification confirms `.github/workflows/build.yml` invokes `node scripts/check-migration-parity.mjs --allow-repo-only` for PR clean-replay preflight and exact parity on main before isolated local Supabase startup/replay. `scripts/check-migration-parity.mjs` unconditionally reads `supabase/LIVE_MIGRATION_MANIFEST.json`, seeds the expected SHA-256 map from `sha256_by_version`, and uses `repo_snapshot_versions` plus `live_exact_from` to classify snapshots and repo-only versions. The base manifest is therefore a required current build/migration-safety input and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. This reference-only classification changes no runtime code/RPC/DB/migration/canonical AC, so synthetic Unit/Integration execution is not required | Done；仅当 migration-parity/build safety responsibility 被正式迁移或替代且 parity script no longer consumes the base manifest 后，再重新创建删除评估切片 |
| V7-REPO-01BD | Migration parity script retention audit | P2 | VERIFIED | independent current-head reference verification confirms both active `.github/workflows/build.yml` and `.github/workflows/integration-contract-tests.yml` directly invoke `node scripts/check-migration-parity.mjs` before isolated local Supabase startup/replay. The script reads `LIVE_MIGRATION_MANIFEST.json` plus optional append manifest, enumerates versioned SQL files, rejects duplicate or missing live versions, classifies repo-only versions from `live_exact_from`/snapshot rules, hashes normalized SQL and exits nonzero on drift. It is therefore a direct current build/integration/migration-safety dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. This reference-only classification changes no runtime code/RPC/DB/migration/canonical AC, so synthetic Unit/Integration or DB execution is not required | Done；仅当 both Build/Integration migration preflight responsibilities are formally migrated/replaced and no current workflow/tooling consumes this script 后，再重新创建删除评估切片 |
| V7-REPO-01BE | Share-cover PNG generator retention audit | P2 | VERIFIED | independent current-head reference verification confirms root `package.json` wires `scripts/generate-share-cover-png.mjs` into both `predev` and `prebuild`, so npm lifecycle executes it before current dev/build paths. The generator uses Node built-ins to write a deterministic 1200×1200 `public/share-cover.png` containing the canonical sage/open-C mark; `test:unit` includes `unit-brand-assets.mjs`, which directly executes the generator and asserts PNG signature, 1200×1200 dimensions, non-trivial raster content and OG/Twitter PNG metadata. It is therefore a direct current dev/build/brand-regression dependency and is not deletion-eligible；retention as `LEGACY_REVIEW` is correct. This reference-only classification changes no runtime code/RPC/DB/migration/canonical AC, so synthetic DB/Integration execution is not required | Done；仅当 package lifecycle/brand regression responsibilities are formally migrated/replaced and current dev/build/unit paths no longer consume this generator 后，再重新创建删除评估切片 |
| V7-REPO-01BF | Source packer retention audit | P2 | NEEDS_VERIFY | affected-scope reference audit confirms root `package.json` exposes `npm run pack` as `node scripts/pack-source.mjs`; the script reads all nine current `bundle/chunk00..08.txt` restore-bundle parts, reconstructs the tracked deployment-source file set, rewrites the nine chunks and `source.bundle.b64`, and is therefore the explicit maintenance counterpart to the currently retained restore/package artifacts. It is a current repository packaging/restore-maintenance dependency and is not deletion-eligible；retain as `LEGACY_REVIEW`. This slice changes no runtime code/RPC/DB/migration/canonical AC, so synthetic product Unit/Integration execution is not required beyond the direct package→script→bundle reference regression | Independent Verifier confirm current-head reference chain and retention classification；Builder must not mark VERIFIED |
| V7-REPO-01AA | Repo remaining dead/legacy artifact cleanup | P2 | TODO | restore bundle、layered CSS、`source.bundle.b64`、AUD-015、Exploratory Browser workflow/script、Candidate Browser、post-deploy backlog note、PR #22 release-blocker control note、audit backlog degraded snapshot、post-deploy remediation scope、H5 Build workflow、Integration Contract workflow、Domain Unit workflow、V6 release changelog、V7 work-split coordination note、V6 PRD canonical retention、V7 development changelog retention、Unit Test baseline retention、Brand asset baseline retention、root CHANGELOG retention、Audit Automation Governance retention、Browser Blackbox baseline retention、Release Governance baseline retention、Integration Test baseline retention、Environment baseline retention、Exploratory Blackbox baseline retention、Event Lifecycle baseline retention、Document Governance baseline retention、Interaction baseline retention、next-version product baseline retention、Product baseline retention、P0 Acceptance baseline retention、Photo Album baseline retention、Quick Start baseline retention、User Story acceptance baseline retention、Test Data Governance baseline retention、Visual Design baseline retention、Tournament Presentation baseline retention、V7 Workboard retention、release-candidate deployment marker retention、root README retention、`.env.example` retention、`.vercelignore` retention、`.gitignore` retention、`package-lock.json` retention、`package.json` retention、`vite.config.ts` retention、root `index.html` retention、`public/storage-compat.js` retention、root `tsconfig.json` retention、root `tsconfig.server.json` retention、root `vercel.json` retention、`supabase/LIVE_MIGRATION_MANIFEST_APPEND.json` retention、`supabase/LIVE_MIGRATION_MANIFEST.json` retention、`scripts/check-migration-parity.mjs` retention、`scripts/generate-share-cover-png.mjs` retention 与 `scripts/pack-source.mjs` retention 已分别审计；其他历史 QA/docs/artifacts 候选仍未逐项证明无运行/构建/测试/release/审计追溯依赖 | Builder 后续逐项审计；不能证明则继续标 `LEGACY_REVIEW`，不得盲删 |

## 调度规则

Builder 每轮只领取一个 `CODE_REOPEN` 或 `TODO` 的最小垂直切片；优先级 P0 → P1 → P2。`INFRA_BLOCKED` / `EXTERNAL_BLOCKED` / `BROWSER_PENDING` 不得阻塞其他 actionable work。