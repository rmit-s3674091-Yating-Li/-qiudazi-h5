import { useState } from "react";import { useParams } from "react-router-dom";import { Send } from "lucide-react";import { Avatar, Empty, ErrorNotice, Header, Loading, isRegistrationOpenClient, labels, suggestedLevelDisplay } from "../components/UI";import { explainError, repository, rpc } from "../repositories/supabase";import { useQuery } from "../hooks/useQuery";type Connection={connection_id:string;id:string;nickname:string|null;avatar_url:string|null};type SentInvite={invitee_user_id:string;status:"pending"|"accepted"|"declined"|"cancelled"|"expired";created_at:string};
export function EventFriendInvitePage(){const{id}=useParams();const eventQ=useQuery("event-"+id,()=>repository.event(id!));const connectionsQ=useQuery("connections",()=>rpc<Connection[]>("list_connections"));const invitesQ=useQuery("sent-event-invites-"+id,()=>rpc<SentInvite[]>("list_sent_event_invites",{p_event_id:id}));const[busy,setBusy]=useState<string|null>(null),[error,setError]=useState("");async function invite(profileId:string){setBusy(profileId);setError("");try{await rpc("invite_connection_to_event",{p_event_id:id,p_invitee_user_id:profileId});await invitesQ.refresh();}catch(e){setError(explainError(e));}finally{setBusy(null);}}if(eventQ.loading&&!eventQ.data)return <><Header title="邀请球搭子参赛"/><main className="page"><Loading/></main></>;const e=eventQ.data?.event;if(!e)return <><Header title="邀请球搭子参赛"/><main className="page"><ErrorNotice message={eventQ.error} retry={eventQ.refresh}/></main></>;const sent=new Map((invitesQ.data||[]).map(i=>[i.invitee_user_id,i]));const closed=!isRegistrationOpenClient(e);return <><Header title="邀请球搭子参赛"/><main className="page"><span className="eyebrow">BRING YOUR TENNIS PEOPLE</span><h1>喊球搭子一起来</h1><p className="muted">{e.name} · {labels[e.match_type]} · {suggestedLevelDisplay(e.suggested_level_min,e.suggested_level_max)}</p><div className="notice">{closed?"这场赛事报名已经结束，不能再发送新的参赛邀请。":"这是参赛邀请，不会给你赛事管理权限。对方接受后仍需要自己完成报名；单打不保证你们一定对阵。"}</div><ErrorNotice message={error||connectionsQ.error||invitesQ.error} retry={()=>{void connectionsQ.refresh();void invitesQ.refresh();}}/>{connectionsQ.loading&&!connectionsQ.data?<Loading/>:connectionsQ.data?.length?<div className="stack">{connectionsQ.data.map(c=>{const i=sent.get(c.id);return <div className="card row" key={c.connection_id}><Avatar path={c.avatar_url} name={c.nickname||"球搭子"} size={40}/><div className="grow"><strong>{c.nickname||"球搭子"}</strong><p className="muted small">{i?.status==="pending"?"已邀请 · 等待回应":i?.status==="accepted"?"已接受邀请":i?.status==="declined"?"之前暂不参加":i?.status==="cancelled"||i?.status==="expired"?"可以重新邀请":"我的球搭子"}</p></div>{i?.status==="pending"||i?.status==="accepted"?<span className="muted small">{i.status==="pending"?"等待中":"已接受"}</span>:<button className="secondary" disabled={closed||busy===c.id} onClick={()=>void invite(c.id)}><Send size={15}/>{busy===c.id?"发送中…":closed?"已截止":"邀请"}</button>}</div>})}</div>:<Empty title="还没有可以邀请的球搭子"><p>先在“球搭子们”建立关系，再邀请朋友一起参加公开赛事。</p></Empty>}</main></>}
